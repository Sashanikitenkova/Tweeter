<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>
<body>
  <div class="container">
    <h4 class="page-title">Full Tweet</h4>
        <div class="post-card">
            <p class="post-date"><%= new Date(tweet.createdAt).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></p>
            <p class="post-message"><%= tweet.title %></p>
            <p class="post-message"><%= tweet.tweet %></p>
        </div>
        <div class="btn-group">
            <a href="/delete/tweet/<%= tweet._id %>" class="btn">Delete</a> 
            <a href="/tweet/edit/<%= tweet._id %>" class="btn">Edit Tweet</a>
        </div>
  </div>
</body>
</html>